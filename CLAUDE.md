## Terraform
- When running `terraform plan`, save output to a temp file and inspect from there - do not run multiple plans in parallel without code changes just to inspect different aspects of the same plan
